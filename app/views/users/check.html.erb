<h1>本当に退会してもよろしいですか？</h1>

<p>退会する場合は「退会する」をクリックしてください</p>

<%= link_to "退会しない", user_path(current_user.id) %> - <%= link_to "退会する", user_withdraw_path(current_user.id), method: :delete, data: {confirm: "本当に退会しますか？" } %>
