<div class="container">
   <div class="row">

      <div class="col-xl-2" style="background-color: gray;">
        <%= render 'users/sidebar', user: @user, tags: @tags %><!--サイドバー-->
      </div><!--サイドパーcol-xl-2-->

      <div class="col-xl-10">

        <h4>作業タイトル☆</h4>
        <p><%= @post.title %></p>

         <h4>付けられたタグ</h4>
         <p>
          <% @post.tags.each do |tag| %>
          <%= link_to tag.name, search_tag_path(tag_id: tag.id) %>
          <% end %>
         </p>

        <h4>作業工程</h4>

         <p>
           <a>1. <%= @post.processimages.first.text1 %></a>
             <% if @post.image1.attached? %>
              <%= image_tag @post.image1, size: "200x200" %>
             <% end %>
         </p>

         <p>
           <% if @post.processimages.first.text2.present? %>
            <a>2. <%= @post.processimages.first.text2 %></a>
             <% if @post.image2.attached? %>
              <%= image_tag @post.image2, size: "200x200" %>
             <% end %>
           <% end %>
         </p>

          <p>
           <% if @post.processimages.first.text3.present? %>
           <a>3. <%= @post.processimages.first.text3 %></a>
             <% if @post.image3.attached? %>
              <%= image_tag @post.image3, size: "200x200" %>
             <% end %>
           <% end %>
          </p>

          <p>
           <% if @post.processimages.first.text4.present? %>
           <a>4. <%= @post.processimages.first.text4 %></a>
             <% if @post.image4.attached? %>
              <%= image_tag @post.image4, size: "200x200" %>
             <% end %>
           <% end %>
         </p>

          <p>
           <% if @post.processimages.first.text5.present? %>
           <a>5. <%= @post.processimages.first.text5 %></a>
             <% if @post.image5.attached? %>
              <%= image_tag @post.image5, size: "200x200" %>
             <% end %>
           <% end %>
         </p>

          <p>
           <% if @post.processimages.first.text6.present? %>
           <a>6. <%= @post.processimages.first.text6 %></a>
             <% if @post.image6.attached? %>
              <%= image_tag @post.image6, size: "200x200" %>
             <% end %>
           <% end %>
         </p>

          <p>
           <% if @post.processimages.first.text7.present? %>
           <a>7. <%= @post.processimages.first.text7 %></a>
             <% if @post.image7.attached? %>
              <%= image_tag @post.image7, size: "200x200" %>
             <% end %>
           <% end %>
         </p>

          <p>
           <% if @post.processimages.first.text8.present? %>
           <a>8. <%= @post.processimages.first.text8 %></a>
             <% if @post.image8.attached? %>
              <%= image_tag @post.image8, size: "200x200" %>
             <% end %>
           <% end %>
         </p>

          <p>
           <% if @post.processimages.first.text9.present? %>
           <a>9. <%= @post.processimages.first.text9 %></a>
             <% if @post.image9.attached? %>
              <%= image_tag @post.image9, size: "200x200" %>
             <% end %>
           <% end %>
         </p>

          <p>
           <% if @post.processimages.first.text10.present? %>
           <a>10. <%= @post.processimages.first.text10 %></a>
             <% if @post.image10.attached? %>
              <%= image_tag @post.image10, size: "200x200" %>
             <% end %>
           <% end %>
         </p>


        <h4>参考にしたサイトURL</h4>
         <p>
           <%= @post.reference_site %>
         </p>

        <h4>使用した商品</h4>
         <p>
           <%= @post.products_used %>
         </p>

        <% if current_user.id == @post.user.id %>
         <%= link_to "削除", post_path(@post.id), method: :delete %> -
         <%= link_to "編集", edit_post_path(@post.id) %>
        <% else %>
         <%= link_to "この投稿者の愛車自慢を見る", user_path(@post.user) %>
        <% end %>



       </div><!--メインコンテンツcol-xl-8-->


   </div><!--row-->
</div><!--container-->

